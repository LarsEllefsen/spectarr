services:
  spectarr:
    build: .
    container_name: spectarr
    restart: unless-stopped
    ports:
      - "6969:6969"
    volumes:
      - ./data:/data
    environment:
      - TZ=Europe/Oslo
      - DATA_DIR=/data
