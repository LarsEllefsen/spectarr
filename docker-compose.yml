services:
  spectarr:
    build: .
    container_name: spectarr
    restart: unless-stopped
    ports:
      - "6969:6969"
    volumes:
      - ./config:/config
    environment:
      - TZ=Europe/Oslo

