services:
  spectarr:
    build: .
    container_name: spectarr
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - TZ=Europe/Oslo
      - DATA_DIR=/data
